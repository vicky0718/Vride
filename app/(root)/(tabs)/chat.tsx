import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  return (
    <SafeAreaView className="flex items-center justify-center h-screen">
      <Text className="text-blue-600">Chat</Text>
    </SafeAreaView>
  );
};
export default Chat;
